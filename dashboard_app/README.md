# Enterprise Sales & Operations Intelligence Dashboard App

Run this standalone Streamlit dashboard from this folder:

```bash
cd dashboard_app
pip install -r requirements.txt
streamlit run app.py
```

The app reads from the project `data/` folder one level above this dashboard folder.
